# AI 代发布指南

> **背景与目的**：
> 本项目的发版工具 `publish.js` 是一个强交互的 Node.js 脚本，默认需要用户在终端根据提示一步步输入回车或确认信息。
> 作为 AI 助手，我们无法“边看屏幕边敲回车”，但这不妨碍我们通过 **Shell 标准输入流（Here-Doc 管道）** 机制，一次性将所有选项打包“喂”给脚本，实现完全自动化的“代理一键发版”。

## 💡 AI 发版标准作业流程 (SOP)

当长官（啦沐达）要求发布新版本时，请务必遵循以下步骤：

### 第一步：调查与梳理变动
1. **绝对不要凭空捏造发版日志**。发布库的路径通常为 `../owo_terminal_public`。
2. 使用 `diff` 精准对比源码与发布库的差异：
   ```bash
   diff -rq ../owo_terminal_public . | grep "differ$"
   ```
3. 针对不同文件的具体内容，使用 `diff -u` 逐个排查，精准提取本次发布的 **架构重组、功能新增、UI/样式变化及 Bug 修复** 等实质性改动。

### 第二步：向长官请示与确认
1. 将提取的内容整理为一份格式工整的《发行说明（Release Notes）》草案。
2. **必须将草案发送给长官过目**，并明确询问是否需要补充或修改。
3. **禁止在获得长官同意前擅自执行发布脚本**。

### 第三步：使用 Here-Doc 魔法执行发布
在获得长官对发行说明的最终批准后，使用后台终端运行（需包含 `WaitMsBeforeAsync` 以便耗时编译期间保持异步汇报）以下组合指令。

**🚨 核心黑科技模板：**

```bash
node publish.js --auto --notes "🚀 主要架构与 UI 升级
- 替换为你和长官确认好的 Release Notes 第一条...
- 替换为确认好的第二条...
✨ 核心问题修复
- 修复了 XXX 导致的问题..."
```

### 🧩 模板参数解析（非常重要）

为了不出错，请必须理解上方模板中参数的作用：
1. **`--auto`**：启动自动化免打扰发版，遇到所有的回车确认都将自动跳过，并自动递增小版本号，自动提交代码及 Tag 到 GitHub 远程。
2. **`--notes`**：紧随其后需带有引号包裹的、经过长官确认的多行 Markdown 文本，它将直接被解析写入发行说明中。不再需要旧版的 `y` 和 `EOF` 管道。

---

> **⚠️ 给后续 AI 助手的特别警告**
> 如果 `publish.js` 在未来的迭代中增加了新的交互提问（例如新增了某些确认项），你在代执行前，必须先 `cat publish.js` 看看 `ask()` 方法的顺序，并对上述的回车/Y/N 序列做出相应的增删调整！
