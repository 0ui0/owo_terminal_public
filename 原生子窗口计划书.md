# 🐾 原生子窗口计划书 v0.2（普通 Notice 弹真实窗 · 最小闭环）

> 写给啦沐达长官的作战地图 🗺️✨
> 状态：📋 方案定稿（未动一行业务代码） | 日期：2026-09-08
> 上一版 v0.1 以「App/桌宠开子窗」为目标；经多轮对质，**方向收敛为：先让普通 Notice 也能弹真实 Electron 子窗**，App/桌宠注入链后置。

---

## 🍡 一、一句话需求

> `Notice.launch({ isElectronWindow: true, contentUrl, contentAttrs, ... })`
> 在主窗口页面里发起时 → **不渲染 DOM 虚拟窗口**，而是让后端开一个真实 Electron 子窗口；
> 子窗口加载同一份前端 main.js，靠 URL 里的 `subWinId` 自检进入「子窗模式」，
> 按 `contentUrl` 重建组件、铺满窗口，并完整复用 Notice 的窗口语义（标题栏/按钮/confirm/cancel）。

### 与 v0.1 的本质差异

| | v0.1（已废） | v0.2（本次） |
|---|---|---|
| 发起方 | App/桌宠（后端 ioSocket 注入 20+ 依赖） | **普通前端代码直接调 Notice.launch** |
| content 形态 | 后端下发的工厂模块（天然可寻址） | 普通组件对象 → **必须新增 contentUrl 可寻址约定** |
| contentAttrs | App 数据（JSON 安全） | **实测混合：多数纯数据，少数含函数**（需分类处理） |
| 事件 | 窗口关闭由后端 appClose 兜底 | **confirm/cancel 闭包留主窗 → 需事件转发协议** |

---

## 🔍 二、战前侦察：代码事实快照（全部已验证 ✅）

| # | 事实 | 证据位置 | 含义 |
|---|---|---|---|
| F1 | 主进程只建**一个**窗口 | app.js L74-96 | `new BrowserWindow({frame:false, transparent:true,...})` → 唯一原生窗 |
| F2 | 渲染层被沙箱隔离 | app.js L83-88 | nodeIntegration:false + contextIsolation:true + 极简 preload → 前端碰不到 Electron |
| F3 | 遥控 Electron 唯一通道 = RPC | settingData.js L39-55 | `fnCall(name, params)` → POST /api/cross → crossFunc 动态 import(electron) |
| F4 | crossFunc 是**自动发现**的 | server/apis/cross/api_cross.js | 遍历 crossFuncs/*.js，按 `export default {name,func}` 匹配 → **丢新文件即新能力** |
| F5 | crossFunc 跑在主进程同 node 上下文 | app.js + sysWinControl.js | 能 `import('electron')` → **能 new BrowserWindow** |
| F6 | 主窗 UI 本身是 Notice 实例 | main.js L63 | `Notice.launch({isMainWindow:true,...})` → 主窗=霸屏原生窗+Notice 壳 |
| F7 | Notice 是纯前端虚拟窗管理器 | notice.js 全文 | dataArr / _winConfig / activateWindow… 零 Electron 概念 |
| F8 | **contentAttrs 实测构成** | 11 处调用点 | 多数纯 JSON（listId/uuid/file/url/diffList/chatList）；**5 处混入函数**（onSuccess/onToggleTool/updateListSession）⚠️ |
| F9 | **confirm/cancel 是 tab 顶层字段** | notice.js L232-262 | 非 contentAttrs！标题栏按钮触发 → 回调返回 undefined=自动关窗，其它值=手动控制 |
| F10 | hideBtn 按钮显隐语义 | noticeBox.js L543/L640 | 0=✓+✕；2=仅✕；3=仅✓；confirmWords/cancelWords 自定义文案 |
| F11 | 子窗内容必须可寻址 | vite dev/prod | 组件对象是闭包不可跨进程 → 需模块 URL/注册键 |
| F12 | 主窗有 ioServer socket | ioSocket.js init() | 后端可定向/广播推送；子窗若连 io 会撞 R1 广播 |
| F13 | sysWinControl 只认 wins[0] | sysWinControl.js L9 | 子窗出现后必然误伤 ⚠️ |

---

## 🧠 三、核心设计（先用人话讲 🐱）

### 3.1 总体一句话

> **真实子窗 = 同一份前端的第二个实例**。它靠 URL 参数知道「我是谁、渲染什么、数据在哪」；
> 它和主窗之间只传三样东西：**URL(身份)、Registry(数据)、事件(动作)**。
> 组件对象与函数闭包**永不跨进程**。

### 3.2 七步流程（通俗版）

```
1️⃣  主窗某处：Notice.launch({ isElectronWindow:true, contentUrl:'/src/view/xxx.js',
      contentAttrs:{...}, confirm, cancel, width,height, tip, hideBtn... })
      → Notice 发现要开真窗 → 校验 contentUrl → 把可 JSON 化部分打包成 spec

2️⃣  Notice 把 spec 发往后端：fnCall('sysCreateSubWindow', [spec])
      → 同时把 confirm/cancel 闭包存进【主窗侧回调注册表】(winId → {confirm,cancel})
      → 不渲染任何 DOM，直接 return

3️⃣  后端 sysCreateSubWindow：
      new BrowserWindow(透明无边框，webPreferences 同主窗沙箱)
      sub.__subWinId = spec.winId
      spec(含 contentAttrs) 存入【后端 Registry】(winId → spec)  ← 大对象/数据走这里
      loadURL(主窗同源 base + '?subWinId=' + winId)   ← 身份走 URL

4️⃣  子窗页面：加载同一份 main.js → 第一行读到 subWinId → 进入【子窗模式】
      → 不做主窗初始化(i18n/settingData.pull/Run/m.route 全跳过)
      → 建立最小 socket.io 连接(带 subWinId 标识，专收定向指令)
      → fnCall('sysGetSubWinSpec', [winId]) 从 Registry 拉 spec
      → import(spec.contentUrl) 重建组件 → 喂最小依赖集 → Notice.launch 本地渲染
      → 子窗页面 dataArr 只有自己一个根 → noticeBox 标题栏/按钮/拖拽天然复用

5️⃣  用户在子窗点 ✓ / ✕：
      子窗 noticeBox → 本地 Notice.confirmWindow/cancelWindow
      → 发现该 tab 的 confirm/cancel 是【事件存根】(非真闭包)
      → 存根发 fnCall('sysSubWinEvent',[winId,'confirm',参数])
      → 后端收到 → 定向 emit 给主窗 socket('subwin:event',...)

6️⃣  主窗 ioSocket 收到 subwin:event → 从回调注册表取出真 confirm/cancel 执行
      → 结果经 fnCall('sysSubWinResult',[winId,{close:true/false}]) 回报后端
      → 后端定向 emit 给子窗('subwin:cmd',{cmd:'close'}) 或子窗收到结果自行决定

7️⃣  关窗/清理：
      子窗原生关闭 → 'closed' 事件 → 通知主窗清理回调注册表 + 后端清理 Registry
```

### 3.3 三种「跨进程物」各走各的通道（铁律）

| 东西 | 本质 | 通道 | 原因 |
|---|---|---|---|
| 身份 | winId 字符串 | **URL query** | 页面加载第一瞬间同步可读，零竞态 |
| 数据 | contentAttrs/spec（JSON 安全） | **后端 Registry 中转** | 可大可小、可含嵌套；URL 塞不下大对象 |
| 事件 | confirm/cancel/关窗 | **cbId 存根 + 后端定向转发** | 函数闭包不可序列化，本体永留主窗 |
| 组件 | content | **contentUrl 模块地址** | 对象不可跨进程，只能「重新长出来」 |

> 注意：**contentAttrs 里的函数（onSuccess 等）在 v0.1 直接禁止用于真实窗**——
> 传了就在主窗 console.warn 并忽略；组件若需要回调，应改走「事件字段」协议（见 3.5）。

### 3.4 普通组件如何变成可寻址 contentUrl

主窗页面里大多数组件是**静态 import 的对象**（如 `import ChatCwdConfig from "./ChatCwdConfig.js"`）。
跨进程重建时，子窗必须能 `import(contentUrl)` 到同一模块。v0.1 提供**组件注册表约定**：

```js
// 新文件：www/src/view/common/subWinRegistry.js（轻量）
// 主窗模块侧把自己「登记」成可寻址组件：
import * as subWinRegistry from "./subWinRegistry.js"
subWinRegistry.register("ChatCwdConfig", () => import("./ChatCwdConfig.js"))

// Notice.launch 侧（真实窗场景）：
Notice.launch({
  isElectronWindow: true,
  contentUrl: subWinRegistry.url("ChatCwdConfig"),  // → "/__subwin__/ChatCwdConfig"
  contentAttrs: { listId }
})
```

**v0.1 双模式兼容：**
- 子窗模式用 `contentUrl` 寻址（查注册表 → 动态 import → 得模块）
- 主窗虚拟窗模式继续用 `content` 对象（零改动，向后 100% 兼容）

> 也可以一步到位：让注册表值 = 动态 import 工厂，URL 仅作 key。最终形态由长官拍板（议程 ③）。

### 3.5 confirm/cancel 的「事件字段」协议

现状（F9）：confirm/cancel 是闭包，返回值决定是否自动关窗。
v0.2 真实窗不能传闭包 → **拆成「字段 + 事件」**：

```js
// 调用方写法（真实窗）——函数仍在主窗，只是不再塞进 obj，而是显式命名：
Notice.launch({
  isElectronWindow: true,
  contentUrl: "...",
  onConfirm: async (payload) => { /* 真闭包：留在主窗执行 */ },
  onCancel:  async (payload) => { /* 同上 */ }
})
```

子窗里 noticeBox 点按钮 → 事件转发（第 5-6 步）→ 主窗执行 onConfirm/onCancel →
返回值语义与现状一致：**undefined → 允许关窗；false → 阻止关窗**。

---

## 🧮 四、原子推理链（因为 → 所以 → 依据）

> 用户规范：每条推理必须「因为/所以/依据」三段原子化，一条只含一个依据，多依据必须拆分。

**R1**
- 因为：mithril 组件对象是 JS 闭包，跨进程即丢失
- 所以：子窗无法接收主窗的 content 对象，只能接收可寻址线索并自行重建
- 依据：Electron 多进程内存隔离 + V8 结构化克隆限制（F11 推论）

**R2**
- 因为：渲染层沙箱没有 electron 能力（F2）
- 所以：开真实 BrowserWindow 必须经 crossFunc RPC（F3/F4/F5 推论）
- 依据：app.js L83-88 + api_cross.js 自动发现机制

**R3**
- 因为：URL query 在 loadURL 完成后立即可同步读取，而 socket 需「连接+握手+订阅」三步异步
- 所以：子窗身份(subWinId)放 URL，避免就绪竞态
- 依据：T3 零竞态注入定理（原计划书已证）

**R4**
- 因为：函数闭包不可序列化，而 contentAttrs 实测 5/11 处含函数（F8）
- 所以：contentAttrs 必须做「JSON 安全」校验；函数字段 v0.1 禁用于真实窗
- 依据：结构化克隆算法限制

**R5**
- 因为：confirm/cancel 闭包依赖主窗内存态（comData/chatData/主窗 DOM）
- 所以：回调本体必须留在主窗执行，子窗只发事件、等结果
- 依据：F9 契约（返回值决定关窗）

**R6**
- 因为：子窗页面也会执行同一份 main.js（F6/F7 同构事实）
- 所以：main.js 顶部 subWinId 自检分支可拦截一切主窗逻辑
- 依据：同源 URL 加载同一前端产物

**R7**
- 因为：子窗可能被系统/用户关闭，且主窗可能主动销毁子窗
- 所以：必须建立 winId 定向控制 + 双向清理（注册表/Registry 防泄漏）
- 依据：F13 现状 wins[0] 误伤风险 + R5 状态同步需求

---

## 🗂 五、逐文件改造清单

### 📄 文件 1（新增）server/crossFuncs/sysCreateSubWindow.js
> C1 = R2 + R3：开真实子窗，URL 带 subWinId，spec 存 Registry。

```js
// 依赖：后端 Registry 模块（见文件 1.5）
export default {
  name: 'sysCreateSubWindow',
  func: async (spec) => {
    if (!(typeof process !== 'undefined' && process.versions?.electron)) {
      return { ok: false, msg: '非 Electron 环境' }
    }
    const { BrowserWindow } = await import('electron')
    const [main] = BrowserWindow.getAllWindows()
    if (!main) return { ok: false, msg: '找不到主窗口' }

    // 1. spec 入 Registry（数据中转，winId 为主键）
    registrySet(spec.winId, spec)

    // 2. 建窗（同主窗沙箱配置，透明无边框默认）
    const sub = new BrowserWindow({
      width: spec.width || 400, height: spec.height || 300,
      x: spec.x, y: spec.y,
      frame: false, transparent: spec.transparent !== false,
      alwaysOnTop: !!spec.isPinned, hasShadow: false,
      resizable: !!spec.resizable,
      webPreferences: {
        webviewTag: true, nodeIntegration: false,
        contextIsolation: true,
        preload: /* 与主窗同一份 preload */ undefined // TODO: 复用主窗 preload 路径
      }
    })
    sub.__subWinId = spec.winId

    // 3. 身份进 URL（零竞态）
    const base = main.webContents.getURL().split('#')[0].split('?')[0]
    await sub.loadURL(base + '?subWinId=' + encodeURIComponent(spec.winId))

    // 4. 关窗 → 通知清理（R7）
    sub.on('closed', () => {
      registryDelete(spec.winId)
      // TODO: 通知主窗清理回调注册表（定向 emit）
    })
    return { ok: true, winId: spec.winId }
  }
}
```

### 📄 文件 1.5（新增）server/crossFuncs/sysSubWinRegistry.js
> C1.5 = R4：Registry 读写 + JSON 安全校验。

```js
const registry = new Map()
export function registrySet(winId, spec) { registry.set(winId, spec) }
export function registryGet(winId) { return registry.get(winId) || null }
export function registryDelete(winId) { registry.delete(winId) }
export function isJsonSafe(v, depth = 0) {
  // TODO: 递归校验函数/undefined/循环引用；遇函数返回 false
}
```

### 📄 文件 2（新增）server/crossFuncs/sysGetSubWinSpec.js
> C2 = R4：子窗就绪后从 Registry 拉取 spec（含 contentUrl/contentAttrs）。

```js
export default {
  name: 'sysGetSubWinSpec',
  func: async (winId) => {
    const spec = registryGet(winId)
    if (!spec) return { ok: false, msg: 'spec 不存在或已清理: ' + winId }
    return { ok: true, spec }
  }
}
```

### 📄 文件 3（新增）server/crossFuncs/sysSubWinEvent.js
> C3 = R5：子窗事件上行 → 后端定向转发主窗。

```js
// 依赖 ioServer（F12）：子窗/主窗都连了 socket，按 winId 定向 emit
export default {
  name: 'sysSubWinEvent',
  func: async (payload) => {
    const { winId, event, args } = payload
    // 定向 emit 给主窗：主窗 socket 需带标记 main=true（见文件 5 改造）
    ioServer.io.emit('subwin:event', { winId, event, args, ts: Date.now() })
    return { ok: true }
  }
}
```

### 📄 文件 3.5（新增）server/crossFuncs/sysSubWinResult.js
> C3.5 = R5：主窗事件结果 → 后端定向转发子窗（决定是否关窗）。

```js
export default {
  name: 'sysSubWinResult',
  func: async ({ winId, result }) => {
    // 定向 emit 给子窗 socket（子窗 socket 带 subWinId 标记）
    ioServer.io.emit('subwin:cmd', { winId, cmd: 'eventResult', result })
    return { ok: true }
  }
}
```

### 📄 文件 4（修改）server/crossFuncs/sysWinControl.js —— ID 定向
> C4 = R7：加可选第二参 winId，缺省 wins[0] 向后兼容。

```js
func: async (action, winId) => {
  const { BrowserWindow } = await import('electron')
  const wins = BrowserWindow.getAllWindows()
  let win
  if (winId) {
    win = wins.find(w => w.__subWinId === winId || w.id === Number(winId)) || wins[0]
  } else {
    win = wins[0]   // 向后兼容：主窗场景
  }
  // ...原有 minimize/maximize/close 逻辑
}
```

### 📄 文件 5（修改）www/src/main.js —— 子窗自检分支
> C5 = R6：IIFE 最前拦截 subWinId。

```js
// 放 async IIFE 最顶部、comData.init 之前
const __qs = new URLSearchParams(location.search)
const __subWinId = __qs.get('subWinId')
if (__subWinId) {
  // 子窗模式：最小自举
  // 1. 标记自身 socket（供后端定向 emit）
  // 2. fnCall('sysGetSubWinSpec', [__subWinId]) → spec
  // 3. import(spec.contentUrl)（经 subWinRegistry 解析）→ 组件
  // 4. 喂最小依赖（m/Notice/comData/settingData…）→ 组件 = 工厂(注入)
  // 5. Notice.launch({ ...spec壳, content: 组件, confirm:事件存根, cancel:事件存根 })
  // 6. return // ★ 绝不执行下方主窗逻辑
}
// ── 以下主窗逻辑原封不动 ──
```

### 📄 文件 6（修改）www/src/view/common/notice.js —— launch 分流 + 回调注册
> C6 = R1+R5：isElectronWindow && !isMain → 校验/注册/发 RPC/return。

```js
// launch 入口（gen sign 之后）插入：
if (obj.isElectronWindow && !obj.isMainWindow) {
  if (!obj.contentUrl) {
    console.error('[Notice] 真实子窗必须提供 contentUrl')
    return
  }
  // contentAttrs JSON 安全校验（v0.1 禁函数）
  // 回调注册表：winId → { confirm: obj.onConfirm, cancel: obj.onCancel }
  subWinCallbacks.set(obj.sign, { confirm: obj.onConfirm, cancel: obj.onCancel })
  settingData.fnCall('sysCreateSubWindow', [{
    winId: obj.sign, contentUrl: obj.contentUrl,
    contentAttrs: obj.contentAttrs, width: obj.width, height: obj.height,
    x: obj.x, y: obj.y, isPinned: obj.isPinned,
    transparent: obj.hideOuter !== false, resizable: !obj.hideOuter
  }]).catch(err => console.error('[Notice] 建窗失败:', err))
  return  // 不画 DOM（T5 单窗收敛）
}
```

### 📄 文件 7（新增）www/src/view/common/subWinRegistry.js —— 组件寻址表
> C7 = R1：普通组件 → 可寻址 contentUrl 的桥梁。

```js
const table = new Map()
export function register(name, factory) { table.set(name, factory) }
export function url(name) { return '/__subwin__/' + name }
export async function resolve(name) {
  const factory = table.get(name)
  if (!factory) throw new Error('未注册的子窗组件: ' + name)
  const module = await factory()
  return module.default || module
}
```

### 📄 文件 8（新增）www/src/comData/subWinBridge.js —— 子窗事件桥
> C8 = R5：主窗侧监听 subwin:event → 执行注册回调 → 回报结果。

```js
// 挂到 ioSocket：socket.on('subwin:event', async ({winId, event, args}) => {
//   const cb = subWinCallbacks.get(winId)?.[event === 'confirm' ? 'confirm' : 'cancel']
//   const close = await cb?.(args)   // undefined=允许关
//   settingData.fnCall('sysSubWinResult', [{ winId, result: { close: close !== false } }])
// })
```

### 📄 文件 9（修改）www/src/comData/ioServer.js —— 定向 emit（主进程侧）
> C9 = R5/R7：ioServer 广播升级为可按标记定向，避免子窗收全量广播（R1）。

```js
// sockets 数组里区分身份：
// 主窗 socket.__isMain = true
// 子窗 socket.__subWinId = xxx
// 新增 emitToMain(event, payload) / emitToSub(winId, event, payload)
```

> 🎯 **v0.1 实施顺序**：文件1/1.5 → 文件5 → 文件6 → 文件3/3.5+8+9 → 文件4 → 文件2 → 文件7
> （先打通「开窗→渲染→事件→关窗」最小闭环，再补 Registry 细节与寻址表）

---

## 🚧 六、风险清单（遗留战场）

- [ ] **R1 双 socket 广播**：子窗若连 ioServer，主窗广播会在子窗重复执行 → ioServer 按 __subWinId 分流（文件 9）
- [ ] **R2 透明点击穿透**：透明子窗区域的鼠标事件；桌宠场景需 setIgnoreMouseEvents，普通弹窗暂不需要
- [ ] **R3 关窗退出联动**：window-all-closed（非 mac）会 quit → 子窗存续时主窗关闭的语义要定义
- [ ] **R4 拖拽与缩放**：透明无框子窗靠 -webkit-app-region: drag → 与组件内交互的冲突区
- [ ] **R5 回调注册表泄漏**：子窗异常关闭 → 主窗 subWinCallbacks 必须清理（closed 事件驱动）
- [ ] **R6 dev/prod 寻址差异**：vite dev 动态 import 路径 vs 生产构建产物路径 → 注册表工厂化规避
- [ ] **R7 contentAttrs 函数静默丢弃**：v0.1 策略是 warn+忽略，需确认长官可接受

---

## 📋 七、待长官拍板的决策点

1. **组件寻址形态**：注册表(key→工厂) 还是 直接传模块 URL？推荐前者（dev/prod 通吃，R6）
2. **contentAttrs 函数策略**：v0.1 一律禁用于真实窗（warn 忽略）？还是允许白名单（onSuccess 单独走事件协议）？
3. **confirm/cancel 命名**：继续用 obj.confirm/obj.cancel（主窗闭包被 Notice 代管）还是改名 onConfirm/onCancel 显式事件字段？推荐后者（语义清晰，避免和 tab.confirm 混淆）
4. **子窗是否连 ioServer**：连（可收后端定向推送，事件结果闭环） vs 不连（纯 HTTP 轮询/单向）→ 推荐连，但必须文件 9 分流
5. **hideBtn 语义迁移**：真实子窗标题栏按钮是否与虚拟窗完全一致（含 confirmWords/cancelWords 自定义文案）？推荐一致（F10 语义复用）
6. **主窗侧是否需要幽灵 Tab**：真实窗在 dataArr 里留一条只读记录（便于统一管理/关闭全部）还是完全独立？推荐 v0.1 完全独立

---

> 🐾 计划书 v0.2 完 —— 请长官批示！确认方向与决策点后，我开始按顺序落码实施。
