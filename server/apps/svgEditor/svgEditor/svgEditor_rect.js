// Generated by CoffeeScript 2.7.0
import Element from "./svgEditor_element.js";

import data from "./svgEditor_data.js";

export default (class {
  constructor(config) {
    this.type = "rect";
    this.config = {
      x: 0,
      y: 0,
      w: 0,
      h: 0,
      ...config
    };
  }

  getP1() {
    return {
      x: this.config.x,
      y: this.config.y
    };
  }

  getP2() {
    return {
      x: this.config.x + this.config.w,
      y: this.config.y
    };
  }

  getP3() {
    return {
      x: this.config.x + this.config.w,
      y: this.config.y + this.config.h
    };
  }

  getP4() {
    return {
      x: this.config.x,
      y: this.config.y + this.config.h
    };
  }

  getElements() {
    var elements;
    elements = [[this.getP1(), this.getP2()], [this.getP2(), this.getP3()], [this.getP3(), this.getP4()], [this.getP4(), this.getP1()]].map((points) => {
      return new Element({
        type: "line",
        prop: {
          points: points,
          parentGroup: data.presentGroup
        },
        isChoised: true
      });
    });
    return elements;
  }

});
