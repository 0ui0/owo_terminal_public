🎨 视觉与体验重塑
- 沉浸式菜单栏进化: 对代码编辑器 (`editor`) 和全局顶部菜单栏 (`FileMenu`) 进行了全面重组：
  * 废弃了原本零散硬编码的下拉组件，统一接入高度结构化的 `Notice / ContextMenu` 原生流派菜单
  * 顶栏按钮 (文件、编辑、视图等) 全面引入 3rem 大圆角和背景透明度平滑动画 (`transition: "background 0.2s ease"`)，悬停反馈更跟手，彻底看齐 MacOS/VSCode 的原生体验
  * 精简面板层级：数据导入/导出设置、角色包导入、以及检查更新功能，均被清晰归类整合进新的流式菜单结构
- 全局排版升级: 将系统级英文全局基底字体从无衬线切至极客向等宽编程字体（`Menlo, Monaco, Consolas` 系列优先），使得中英文和终端代码混排的视觉呈现更加严谨有序

✨ 核心 Bug 修复
- SVG 解析引擎抗风险强化: 彻底修复了使用 SVG 编辑器打开外部复杂网点图时，因过滤掉不支持标签 (如空 `<g>` 或未闭合 `<path>`) 而产生的隐形空组，进而导致全图包围盒被硬生生拉扯到坐标原点 `(0,0)` 造成显示错乱的恶性 Bug。现已引入 `cleanEmptyGroups` 后处理净化机制，精准锁定有效内容边界