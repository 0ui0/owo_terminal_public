owo-terminal Release Notes - v1.1.21
协同编辑体验 进行了深度优化与安全拦截升级，同时对 进程管理器（Task Manager） 进行了完全符合开发规范的响应式与配色重构。此版本针对 编辑器 AI 
 (AI Collaborative Features)1. 编辑器 AI 协同与行批注系统
Modified 侧）右键选中行，通过 Notice 弹窗输入行级修改意见，并在编辑器底部渲染横向可折叠、带清除按钮的批注小标签。右键行批注：支持在 Monaco 编辑器（
会优雅展示 AI 提供的“修改理由（Reason）”（支持最大高度限制与纵向滚动，防止撑开视窗）。修改理由提示条：在 AI 调起 Diff 对比窗口时，界面顶部栏下方
发送 Diff 详情：顶部工具栏新增“发送 Diff”开关。选中后，批准修改时会自动将 Monaco 引擎计算出的差异行（Unified Diff 格式）拼接入命令备注中回传给 AI，帮助 AI 更好理解代码变动。
滑滚动定位并居中跳转至下一处代码差异块。差异跳转导航：Diff 模式右下角引入毛玻璃半透明导航胶囊，支持点击 ◀ 和 ▶ 平
2. 进程管理器 (Task Manager) 响应式重构
承并完美对齐宿主框架的主题风格、边框与圆角规范。Box / Tag 规范化：完全重构进程卡片（Box）与控制键组（Tag），去除所有硬编码样式，继
片完全铺满窗口，并将刷新按钮以 headerButtons 的形式动态迁移至窗口标题栏右侧，极致节省垂直空间。首屏精简：去除了冗余的进程管理器头尾布局，使进程卡
态时（宽度 < 500px）自动切换为垂直布局，防按钮挤压重合。响应式适配：引入 ResizeObserver 动态计算容器宽度。在拉伸至移动端/窄屏状
占位升级为拉伸填满的真实 SVG 进程图标，并带有 onerror 降级渲染首字母的安全兜底机制。真实 SVG 图标：进程图标由字母
🐛 缺陷修复 (Bug Fixes)
加载将丢失修改”警告框时，因 appActive 异步置顶事件较慢反而后发覆盖将警告框压在身下的 Bug。通过 await fnCall 链式调用与 sleep 100ms 延迟，确保警告框始终浮在最上层。提示窗口遮挡修复：修复了在单例查重检测到外部修改并弹出“重新
top"（实际应为 "appClose"）导致后端 editor 实例未释放的 Bug，彻底消除任务管理器中的僵尸后台进程。单例幽灵实例残留修复：更正了前端在查重命中并闪退销毁临时新实例时，因方法名错写为 "appS
免批准修改后自动弹窗查重与刚刚关闭的 Diff 窗口发生竞态，防止正常编辑器被误杀闪退。Diff 查重闪退死锁修复：在查重过滤中增加了 isDiff 过滤拦截，避
盖与刷新机制：修复了“加载外部修改”后保存仍报冲突的 Bug，将其调整为执行 open 动作彻底重新拉取磁盘内容并重置 mtime。保存冲突覆